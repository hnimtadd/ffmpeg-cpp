#pragma once

class Client {
public:
  /*
   * Default constructor for Calculator
   */
  Client();
  /**
   * Calculate sum of a and b.
   * @return sum of a and b.
   */
  void ShowImage(char *path);
  void StreamVideo(char *path, char *c_name);
};
